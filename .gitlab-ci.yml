pages:
  image: node:16-alpine
  stage: deploy
  script:
    - yarn install
    - yarn build
    - yarn doc
    - yarn export
    - mv packages/daw/out ./public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
